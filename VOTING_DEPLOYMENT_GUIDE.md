# 🚀 投票平台部署指南

## 📋 部署步骤

### 1. 环境准备

确保已配置环境变量：

```bash
# packages/contracts/.env
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_KEY
PRIVATE_KEY=your_private_key_here
```

### 2. 编译合约

```bash
cd packages/contracts
npm run compile
```

### 3. 部署到 Sepolia 测试网

```bash
npm run deploy:voting:sepolia
```

部署成功后，会显示合约地址，例如：
```
✅ 投票合约部署成功！
合约地址: 0x1234567890123456789012345678901234567890
网络: sepolia
```

### 4. 部署到本地网络（测试用）

```bash
# 启动本地节点
npx hardhat node

# 在另一个终端部署
npm run deploy:voting:local
```

### 5. 更新前端配置

将部署的合约地址添加到前端：

**方法1：更新预设地址**
编辑 `packages/frontend/src/components/ContractAddressSelector.tsx`：
```typescript
{
  name: '🗳️ 投票平台（Sepolia）',
  address: '0x你的合约地址', // 更新这里
  description: '投票平台合约地址',
  recommended: true,
  type: 'voting',
}
```

**方法2：在前端界面输入**
1. 启动前端：`cd packages/frontend && npm run dev`
2. 连接钱包
3. 在"合约地址配置"中输入部署的合约地址
4. 点击"设置"

## 🧪 测试合约

### 运行测试

```bash
cd packages/contracts
npm test
```

### 手动测试

1. **创建提案**
   - 在前端填写提案标题和描述
   - 选择投票时长
   - 点击"创建提案"

2. **投票**
   - 在提案卡片中选择"支持"或"反对"
   - 点击投票按钮
   - 确认交易

3. **查看结果**
   - 等待投票结束
   - 结果会自动显示

## 📊 合约功能验证

### 使用 Hardhat Console

```bash
npx hardhat console --network sepolia
```

```javascript
// 获取合约实例
const PrivacyVoting = await ethers.getContractFactory("PrivacyVoting");
const voting = await PrivacyVoting.attach("0x你的合约地址");

// 获取提案数量
const counter = await voting.proposalCounter();
console.log("提案数量:", counter.toString());

// 获取提案信息
const proposal = await voting.getProposal(0);
console.log("提案标题:", proposal.title);
console.log("提案描述:", proposal.description);
console.log("投票数量:", proposal.voteCount.toString());
```

## 🔍 验证合约（可选）

如果部署到公共测试网，可以验证合约：

```bash
npx hardhat verify --network sepolia 0x你的合约地址
```

## 📝 部署检查清单

- [ ] 环境变量已配置
- [ ] 合约编译成功
- [ ] 合约部署成功
- [ ] 合约地址已保存
- [ ] 前端配置已更新
- [ ] 测试创建提案
- [ ] 测试投票功能
- [ ] 测试结果显示

## 🐛 常见问题

### 1. 部署失败：Insufficient funds
**解决**：确保账户有足够的 Sepolia ETH

### 2. 部署失败：Network error
**解决**：检查 RPC URL 是否正确

### 3. 前端无法连接合约
**解决**：
- 检查合约地址是否正确
- 检查网络是否匹配（Sepolia）
- 检查合约 ABI 是否正确

### 4. 投票失败：Already voted
**解决**：每个地址只能投票一次，这是正常行为

## 🎉 部署完成

部署成功后，您可以：

1. ✅ 在前端创建提案
2. ✅ 进行加密投票
3. ✅ 查看投票结果
4. ✅ 体验加权投票（如果启用）

**祝您使用愉快！** 🚀




