# 📝 设置合约地址指南

## 🎯 快速设置

### 方法1：在代码中直接设置（推荐用于开发）

编辑 `packages/frontend/src/app/page.tsx`，找到这一行：

```typescript
const [contractAddress, setContractAddress] = useState<string>('0x0000000000000000000000000000000000000000');
```

将 `'0x0000000000000000000000000000000000000000'` 替换为您的合约地址，例如：

```typescript
const [contractAddress, setContractAddress] = useState<string>('0x您的合约地址');
```

### 方法2：通过前端界面设置

1. 启动前端：`cd packages/frontend && npm run dev`
2. 连接钱包
3. 在"合约地址配置"区域输入合约地址
4. 点击"设置"按钮

### 方法3：使用预设地址

编辑 `packages/frontend/src/components/ContractAddressSelector.tsx`，在 `PRESET_ADDRESSES` 数组中更新：

```typescript
{
  name: '🗳️ 投票平台（Sepolia）',
  address: '0x您的合约地址', // 更新这里
  description: '投票平台合约地址',
  recommended: true,
  type: 'voting',
}
```

## 🚀 部署合约

如果还没有部署合约，请先部署：

```bash
cd packages/contracts
npm run deploy:sepolia
```

部署成功后，会显示合约地址，复制该地址并按照上述方法设置。

## ✅ 验证设置

设置合约地址后：
1. 刷新页面
2. 连接钱包
3. 应该能看到投票平台界面
4. 如果链上有提案，会自动显示

## 📋 当前状态

- **默认地址**: `0x0000000000000000000000000000000000000000`（无效地址，需要替换）
- **需要操作**: 部署合约后，将地址更新到代码中


