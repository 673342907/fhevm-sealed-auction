# 🚀 生产环境部署检查清单

## ✅ 部署前检查

### 1. 代码检查
- [x] 已修复 `createTransaction` 未定义错误
- [x] 默认合约地址已设置为 Sepolia 测试网地址
- [x] 无硬编码的 localhost 地址（仅作为预设选项）
- [x] FHEVM 使用动态 chainId，支持多网络

### 2. 网络配置
- ✅ **Sepolia 测试网**：默认合约地址 `0x532d2B3325BA52e7F9FE7De61830A2F120d1082b` 已部署
- ✅ **FHEVM 支持**：自动检测 MetaMask 的网络和 chainId
- ✅ **多网络支持**：通过 MetaMask 切换网络即可

### 3. 依赖项检查
- ✅ Next.js 14.0+ (支持静态导出)
- ✅ React 18.2+
- ✅ ethers.js 6.8+
- ✅ @fhenixprotocol/fhevmjs (FHEVM SDK)

### 4. 构建配置
- ✅ `next.config.js` 已配置 webpack fallback
- ✅ 支持浏览器环境（无 Node.js 特定代码）

## 📦 部署步骤

### 方法 1: Vercel 部署（推荐）

1. **准备代码**
   ```bash
   # 确保代码已提交到 Git
   git add .
   git commit -m "Ready for deployment"
   git push
   ```

2. **在 Vercel 部署**
   - 访问 [vercel.com](https://vercel.com)
   - 导入 GitHub 仓库
   - 设置项目：
     - **Framework Preset**: Next.js
     - **Root Directory**: `packages/frontend`
     - **Build Command**: `npm run build`
     - **Output Directory**: `.next`
   - 点击 Deploy

3. **环境变量（可选）**
   - 如果需要自定义合约地址，在 Vercel 项目设置中添加：
     - `NEXT_PUBLIC_CONTRACT_ADDRESS` (可选)

### 方法 2: Netlify 部署

1. **构建配置**
   创建 `netlify.toml` 在项目根目录：
   ```toml
   [build]
     base = "packages/frontend"
     command = "npm run build"
     publish = "packages/frontend/.next"
   
   [[redirects]]
     from = "/*"
     to = "/index.html"
     status = 200
   ```

2. **部署**
   - 连接 GitHub 仓库
   - 设置构建目录为 `packages/frontend`
   - 部署

### 方法 3: 自托管服务器

1. **构建项目**
   ```bash
   cd packages/frontend
   npm install
   npm run build
   ```

2. **启动生产服务器**
   ```bash
   npm start
   ```

3. **使用 PM2（推荐）**
   ```bash
   npm install -g pm2
   pm2 start npm --name "voting-platform" -- start
   ```

## ⚠️ 注意事项

### 1. FHEVM 网络支持
- ✅ **Sepolia 测试网** (Chain ID: 11155111) - 已测试
- ⚠️ **主网** - 需要确认 FHEVM 是否支持
- ⚠️ **其他测试网** - 需要确认 FHEVM 支持

### 2. MetaMask 要求
- 用户必须安装 MetaMask 浏览器扩展
- 用户需要切换到支持的网络（Sepolia 测试网）
- 用户需要连接钱包

### 3. 合约地址
- 默认使用已部署的 Sepolia 合约地址
- 用户可以在界面中更换合约地址
- 如果部署新合约，更新 `ContractAddressSelector.tsx` 中的预设地址

### 4. 浏览器兼容性
- ✅ Chrome/Edge (推荐)
- ✅ Firefox
- ✅ Safari (需要测试)
- ⚠️ 移动端浏览器（MetaMask Mobile 支持）

## 🔍 部署后验证

1. **访问部署的网站**
   - 检查页面是否正常加载
   - 检查样式是否正确

2. **连接钱包**
   - 点击"连接钱包"按钮
   - 在 MetaMask 中确认连接
   - 切换到 Sepolia 测试网

3. **测试功能**
   - 查看提案列表（应该能看到已部署的提案）
   - 创建新提案
   - 参与投票
   - 查看投票结果

4. **检查控制台**
   - 打开浏览器开发者工具
   - 检查是否有错误
   - 检查网络请求是否正常

## 🐛 常见问题

### 问题 1: FHEVM 初始化失败
**原因**: 网络不支持或 chainId 错误
**解决**: 确保 MetaMask 连接到 Sepolia 测试网 (Chain ID: 11155111)

### 问题 2: 无法加载提案
**原因**: 合约地址错误或网络不匹配
**解决**: 
- 检查合约地址是否正确
- 确认 MetaMask 网络与合约部署网络一致

### 问题 3: 交易失败
**原因**: Gas 费不足或网络拥堵
**解决**: 
- 确保钱包有足够的测试网 ETH
- 等待网络拥堵缓解后重试

### 问题 4: 样式显示异常
**原因**: CSS 未正确加载
**解决**: 
- 检查构建输出
- 确认 Tailwind CSS 配置正确

## 📝 部署清单总结

✅ **可以部署到生产环境**
- 代码无硬编码的本地依赖
- 使用 MetaMask 作为钱包提供者（浏览器原生）
- FHEVM 通过浏览器动态初始化
- 默认合约地址已部署到 Sepolia 测试网
- Next.js 支持静态导出和服务器端渲染

⚠️ **需要注意**
- 确保用户使用支持的浏览器（Chrome/Edge/Firefox）
- 用户需要安装 MetaMask
- 用户需要切换到 Sepolia 测试网
- 如果部署到主网，需要确认 FHEVM 支持





